import { userName, firstName, lastName } from "./Snippet_84.js";
let app = document.getElementById("app");
let html = firstName + " " + lastName + "<br> Is testing import and export as the username:<br>" + userName;
app.innerHTML = html;